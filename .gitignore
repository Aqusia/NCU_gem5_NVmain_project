# Cloned + built trees (regenerated by setup.sh; large)
/_work/

# env.sh is regenerated per machine by setup.sh; a template is checked in.
# Comment the next line out if you want to commit a custom env.sh.
#env.sh

# gem5 build + simulation artifacts
build/
m5out/
*.pyc
parsetab.py
parser.out

# gem5 per-run config dumps (gem5sim already deletes these for saved runs)
config.ini
config.json

# Compiled benchmark binaries (gem5sim recompiles them from the .c sources)
/data/benchmarks/quicksort
/data/benchmarks/multiply

# Container scratch / editor noise
*.tar
*.swp
.DS_Store
